#Kubernetes is a system that helps run, manage, and scale apps automatically — like a smart manager for your app's servers.

minikube start --driver=docker
minikube start -p aged --kubernetes-version=v1.16.1 #Create a second cluster running an older Kubernetes releas
minikube start


minikube start --nodes==2   #Start minikube cluster with 2 dones  default=1(Control Plane)
kubectl get nodes -A    #Get all nodes

kubectl get po -A      # GIve detail of all the running pods
ubectl get pods  
minikube kubectl -- get po -A

minikube dashboard

minikube pause
minikube unpause
minikube stop


minikube image list
minikube image load <docker_image_name>    #Push Docker image to minikube cluster
kubectl apply -f <deployment.yaml>         #Deployment
minikube service <app name in yaml file>   #To run the app


kubectl delete pod <fist-kubernetes-app-5df5749945-zbl7j>    # Delete pods
kubectl delete deployment <flask-app:latest>                 # Delete deployment
minikube delete --all

kubectl get endpoints

#Load Balancing
kubectl get pods
kubectl logs -f <fist-kubernetes-app-5df5749945-7wxgh>






#----------------------------Best---------------------------------
minikube start
minikube image list
minikube image load <docker_image_name>    #Push Docker image to minikube cluster
kubectl apply -f <deployment.yaml>         #Deployment
minikube service <app name in yaml file>   #To run the app